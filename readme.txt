This is a demo project to start implementing OpenGL applications
using freeglut. 

To use, simply open the file in xCode and build + run.

Simple painting application with minor bugs.

Window Resize issue:
 - Scaling off, if window resized with mouse, appropriate values snap back
Size of drawing tool must be selected numerically usig key board values 1,2,3.
